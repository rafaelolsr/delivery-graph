# Assets

Plugin icons, diagrams, screenshots, and marketplace assets live here.

- `banner.svg` — project banner (source; GitHub renders SVG inline).
- `banner.png` — 2× raster of the banner, referenced by `README.md` for max compatibility.
  Regenerate from the SVG with a headless Chromium screenshot at 1280×360.

Runtime delivery assets are not saved here. They are written to the consuming repository under `delivery-graph/`.

