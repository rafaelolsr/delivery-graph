# Examples

Example delivery graphs, generated tracker projections, and validation evidence examples live here.

Run:

```bash
npm run check
```

The command validates `delivery-graph.example.json` and renders its status.
